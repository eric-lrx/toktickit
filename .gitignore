# dependencies
node_modules/
# env & secrets
.env
*.env
!.env.example
# build output
dist/
build/
# prisma
server/prisma/*.db
# uploaded attachments (Lab 2, Issue 11)
server/uploads/
# Playwright run output (Lab 2, Issue 12) — not the artifacts/lab-02/screenshots
# evidence itself, which is a required deliverable and stays tracked.
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
# logs & OS
*.log
.DS_Store
